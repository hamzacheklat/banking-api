import anyio
from typing import Coroutine, Any

class AsyncRunner:
    @staticmethod
    def run(coroutine: Coroutine[Any, Any, Any]) -> Any:
        """
        Run a coroutine safely in both sync and async contexts using anyio.
        Automatically handles event loop logic for sync environments.
        """
        return anyio.run(lambda: coroutine)
